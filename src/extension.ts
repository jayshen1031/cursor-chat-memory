import * as vscode from 'vscode'; export function activate(context: vscode.ExtensionContext) { console.log('🧠 Activated'); const statusBarItem = vscode.window.createStatusBarItem(vscode.StatusBarAlignment.Right, 100); statusBarItem.text = '🧠 Memory'; statusBarItem.show(); const cmd = vscode.commands.registerCommand('cursorChatMemory.enhanceInput', async () => { vscode.window.showInformationMessage('📋 Demo working!'); }); context.subscriptions.push(cmd, statusBarItem); vscode.window.showInformationMessage('🧠 Cursor Chat Memory is active!'); } export function deactivate() { }
